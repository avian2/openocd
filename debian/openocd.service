[Unit]
Description=OpenOCD sysfs GPIO setup
Requires=systemd-udevd.service
After=systemd-udevd.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c 'for n in 23 48 66 67 68 69; do echo $n > /sys/class/gpio/export; done'
ExecStop=/bin/sh -c 'for n in 23 48 66 67 68 69; do echo $n > /sys/class/gpio/unexport; done'

[Install]
WantedBy=multi-user.target
